{
    "name": "userforged/shop-engine",
    "description": "Ordering engine (catalog, cart, checkout) — canonical shop pattern, ports-based, framework-agnostic core.",
    "type": "symfony-bundle",
    "license": "proprietary",
    "autoload": {
        "psr-4": {
            "Userforged\\ShopEngine\\": "src/"
        }
    },
    "require": {
        "php": ">=8.5",
        "psr/event-dispatcher": "^1.0",
        "psr/log": "^3.0",
        "symfony/config": "^8.1",
        "symfony/dependency-injection": "^8.1",
        "symfony/http-kernel": "^8.1",
        "symfony/messenger": "^8.1",
        "symfony/uid": "^8.1",
        "symfony/workflow": "^8.1",

        "doctrine/dbal": "^4.4",
        "doctrine/doctrine-bundle": "^3.3",
        "doctrine/orm": "^3.6"
    },
    "require-dev": {
        "symfony/framework-bundle": "^8.1"
    },
    "suggest": {
        "symfony/framework-bundle": "To register UserforgedShopEngineBundle — a Symfony bundle is an adapter, not the core; the engine itself needs no framework."
    },
    "_comment_require": [
        "The second group is transitional, not part of the target surface.",
        "doctrine/* is only needed because the write side still traverses Doctrine",
        "directly (EntityManagerInterface, OrderLinesType). symfony/http-foundation is",
        "gone: CartStorageInterface now ships as the cart persistence port, and the",
        "library no longer touches the HTTP session directly. Declared meanwhile",
        "because a manifest that understates its dependencies is worse than one that",
        "admits the debt."
    ]
}
