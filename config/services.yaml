parameters:

services:
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    App\:
        resource: '../src/'

    # userforged/shop-engine now registers its own services via its bundle,
    # scanning only packages/userforged/shop-engine/src/. That scan never sees
    # these App\ classes, so the automatic "singly implemented interface"
    # aliasing (which used to bridge port -> adapter when both lived in this
    # same services.yaml) no longer fires across the bundle boundary — these
    # aliases restore it explicitly.
    Userforged\ShopEngine\CartStorageInterface: '@App\Game\Shop\SessionCartStorage'
    Userforged\ShopEngine\OrderRepositoryInterface: '@App\Repository\OrderRepository'
    Userforged\ShopEngine\BuyerProviderInterface: '@App\Game\Shop\PlayerBuyerProvider'
    Userforged\ShopEngine\FulfillmentInterface: '@App\Game\Shop\AdvanceFulfillment'
    Userforged\ShopEngine\ProductProviderInterface: '@App\Game\Shop\AdvanceProductProvider'
    Userforged\ShopEngine\FacetProviderInterface: '@App\Game\Shop\ShopConnector'
    Userforged\ShopEngine\PriceResolverInterface: '@App\Game\Shop\AdvancePriceResolver'

when@test:
    services:
        Symfony\Component\Mercure\HubInterface: '@App\Tests\Support\Mercure\RecordingHub'
        App\Tests\Support\Mercure\RecordingHub:
            public: true
