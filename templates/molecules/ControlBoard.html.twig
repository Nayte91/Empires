<details {{ attributes }} class="control-board panel" open>
    <summary class="label">Controls</summary>

    <div class="controls">
        {% for stat in ['cities', 'ships', 'census', 'treasury', 'cards'] %}
            <twig:molecules:StatPicker :player="player" :stat="stat" :value="attribute(player, stat)" />
        {% endfor %}

        {% if withShopLink %}<a href="{{ this.shopUrl }}" class="button shop-link">Shop</a>{% endif %}
    </div>
</details>
