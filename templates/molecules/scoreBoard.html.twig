<table
    {{ attributes.defaults(stimulus_controller('mercure-refresh', {topic: 'empires/game/' ~ game.id, events: ['player-updated', 'game-updated']})) }}
    class="score-board"
>
    <caption id="scoreboard">Turn {{ game.currentTurn }}</caption>
    <thead>
        <tr>
            <th scope="col">Name</th>
            <th scope="col" data-empire>Empire</th>
            <th scope="col">Treasury</th>
            <th scope="col">Census</th>
            <th scope="col">Cities</th>
            <th scope="col">Cards</th>
            <th scope="col">Techs</th>
        </tr>
    </thead>
    <tbody>
        {% for row in this.playerRows %}
            {% set player = row.player %}
            <tr data-empire="{{ player.empire }}">
                <td data-name>
                    <twig:molecules:Modal class="game-dashboard__name-modal">
                        <twig:block name="button">
                            {{ player.name }}
                        </twig:block>
                        <twig:block name="content">
                            <h2>{{ player.name }}</h2>
                            {{ row.qr|raw }}
                            <p class="game-dashboard__qr-url"><a href="{{ row.url }}">{{ row.url }}</a></p>
                            <button type="button" {{ stimulus_action('modal', 'close') }}>Close</button>
                        </twig:block>
                    </twig:molecules:Modal>
                </td>
                <td data-empire>
                    {{ this.empireAdjective(player) ?? '—' }}
                    {% if row.military %}<small title="Military">⚔</small>{% endif %}
                </td>
                <td>{{ player.treasury }}</td>
                <td>{{ player.census }}</td>
                <td>{{ player.cities }}</td>
                <td>{{ player.cards }}</td>
                <td>{{ player.advances|length }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
