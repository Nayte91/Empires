{% props title = '', subtitle = '' %}

{% if title is not empty %}
<hgroup id="page-title">
	<h1>{{ title }}</h1>
	{% if subtitle is not empty %}
		<p>{{ subtitle }}</p>
	{% endif %}
</hgroup>
{% endif %}
