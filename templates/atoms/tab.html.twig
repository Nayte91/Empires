<details
    class="tab"
    name="{{ group }}"
    data-tab-id="{{ tabId }}"
    {{ open|default(false) ? 'open' }}
    data-empire="{{ empire|default }}"
>
    <summary>{{ label }}</summary>
    {% block panel %}{% endblock %}
</details>
