<section class="catalog">
    <h2>Catalog</h2>

    {% for row in this.rows %}
        {% include 'molecules/productTile.html.twig' with {row, locked, action: 'add', remainingBudget} only %}
    {% endfor %}
</section>
