<section class="catalog">
    <h2>Catalog</h2>

    {% for row in this.rows %}
        {% include 'molecules/productTile.html.twig' with {row, locked: view.locked, remainingBudget: view.remainingBudget, action: 'add'} only %}
    {% endfor %}
</section>
